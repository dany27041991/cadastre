/**
 * App router: placeholder for future route definitions.
 * Renders children as the single "route" until react-router is added.
 */
import type { ReactNode } from 'react'

export function Router({ children }: { readonly children: ReactNode }) {
  return <>{children}</>
}
