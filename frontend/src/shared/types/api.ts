/**
 * Shared API-related types (generic request/response, pagination, etc.).
 */
// TODO: add generic API types when needed (e.g. PaginatedResponse, ApiError)
export type ApiError = { message: string; code?: string }
