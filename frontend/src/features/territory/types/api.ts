/**
 * Territory API contracts (for navigation and map).
 * Re-exports from api layer; single source of truth for API types.
 */
export type {
  GreenAreasParams,
  GreenAssetParams,
  TerritoryGeoApi,
  TerritoryGreenAreasApi,
  TerritoryNavigationApi,
} from '../api/territory.api'
